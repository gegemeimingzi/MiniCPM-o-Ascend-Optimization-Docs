# 项目总结

> 赛道：昇腾 AI 创新大赛 · 子赛道 A（`llama.cpp-omni` 推理优化）
> 队伍：doma · 硬件：Ascend 910 ×2（CANN 9.1.0-beta.1）· OS：Ubuntu 22.04 aarch64
> 代码仓库：`github.com/tc-mb/llama.cpp-omni`（分支 `bench/huawei`，基于官方 `b06198f`）

## 1. 项目是什么

**llama.cpp-omni** 是基于 llama.cpp 的全双工（duplex）全模态推理引擎，运行 MiniCPM-o 4.5（9B 端侧全模态模型），把**视觉理解、语音识别、LLM 推理、语音合成（TTS）、声码器（Token2Wav）**整合成一条实时管线，支持"边说边看边回答"的实时视频通话。

本赛道目标是**在 Ascend 910 上把 RTF（实时率，<1.0 达标）尽量压低**。

## 2. 模型与管线（五大 GGUF 模块）

| 模块 | 缩写 | 架构 | 职责 |
|---|---|---|---|
| 视觉编码器 | VPM | SigLip2 + Resampler | 图像 → 视觉 embedding |
| 音频编码器 | APM | Whisper | 音频 → 音频 embedding |
| 主语言模型 | LLM | Qwen3-8B | 自回归生成文本 token |
| 语音合成 | TTS | LLaMA | LLM 隐状态 → 音频 token |
| 声码器 | Token2Wav | Flow Matching | 音频 token → 24kHz 波形 |

## 3. 赛题口径

- **性能主指标**：`SPEAK→WAV` 完整链路 **RTF**（core 帧，pooled 聚合），官方 F16 基线 `1.087`（910C）。
- **精度准入**（相对 F16 基线降幅 ≤2pp / ≤10%）：
  - VideoMME 基线 69.0，准入 ≥67.0
  - Daily-Omni 基线 79.5，准入 ≥77.5
  - TTS-SEED：ASV ≥0.689（基线 0.709）；WER ≤1.56（基线 1.414）

## 4. 优化历程概览

1. **910B3 → 910C**：换服务器，基线 1.369 → 1.0086。
2. **主 LLM FlashAttention**：decode -56%，RTF 0.835（RTF<1 达成）。
3. **量化证伪**：Q4/Q8/W8A8 全部因 CANN launch 墙失败。
4. **TTS 阶段优化**：TTS FA（decode -7~10%）、head_code NPU（CPU→NPU matmul）。
5. **Token2Wav 优化**（RTF 大头）：
   - t2m 冗余 CONT 消除（Sprint 7 B-lite / Sprint 13 源码层，bit-identical）
   - 4 步 flow matching、VPM 批量编码、vocoder F16（否决）
   - flow B=1（-16% 但 TTS 精度评测崩溃，否决）
6. **官方评测对齐**：修复 stale binary、ACL_GRAPH 双工退化、funasr 路径、评测口径一致（`b06198f`）。

## 5. 关键结论

- **RTF 已从 1.369 降到 ~0.69（full）/ core 预期 ~0.70**，相对官方基线 -35~-40%。
- **瓶颈是 CANN kernel launch 墙**（decode/prefill 每 op ~13μs dispatch），而非带宽；算子融合/executor 复用受 CANN API 限制基本走不通。
- **精度全程零回归**（所有保留优化均 bit-identical 或官方验证过）。

## 6. 最终交付

- 提交包：`submission.zip`（README + demo.mp4 + llama.cpp-omni.zip + integration-support.zip）
- 代码：`llama.cpp-omni` `bench/huawei` @ `48076a8`（base `b06198f`）
- 实验归档：本仓库（`优化成果.md` + `优化实验/` + `历史归档/`）
