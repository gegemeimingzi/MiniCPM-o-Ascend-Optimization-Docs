# 优化成果（活文档）

> 汇总 MiniCPM-o 昇腾子赛道 A 的 RTF 优化历程、最终配置与保留/否决项。更新它而不是每次新建文件。

## 1. RTF 演进总览

| 阶段 | 日期 | 配置 / 事件 | RTF（口径） | 说明 |
|---|---|---|---|---|
| 910B3 基线 | 08-13 | F16 全量 | **1.369** | 910B3 单卡；stage: tts0.41+decode0.37+encode0.31+t2w0.27 |
| 910C 基线 | 08-14 | F16 干净 | **1.0086** | decode 0.318 + tts 0.259 + t2w 0.243 + encode 0.176 |
| 主 LLM FA | 08-14 | `OMNI_FLASH_ATTN` 默认开 | **~0.835** | decode 0.318→0.138（**-56%**），RTF<1.0 达成 |
| Sprint 5 | 08-15 | TTS FA | TTS 阶段 -7.6% | decode/token -7~-10%；SPEAK→WAV 持平（T2W 主导） |
| Sprint 7 | 08-15 | t2m B-lite（删冗余 CONT） | t2m -7.7%、RTF -6.8% | 340 个冗余 CONT，bit-identical |
| Sprint 13 | 08-16 | CONT 源码层消除 | t2m -3.9%、RTF -2.5~5% | ADD+MUL 335 个 bit-identical；CONCAT 排除 |
| Sprint 16 | 08-20 | 官方复测修正 | **core 0.762**（-29.9%） | 修复 stale binary + ACL_GRAPH 双工退化 |
| 本轮 | 08-20~21 | TTS FA + VPM batch + 4步 flow | **FULL 0.694**（core 预期 ~0.70） | 相对官方 1.087 约 -35~-40% |

> 口径说明：官方主指标为 judge 双工 `rtf.core.rtf_aggregate`（掐头去尾、pooled）。单样例自测仅 2 个 core 帧，波动大；FULL（10 帧）更稳定。

## 2. 最终提交配置（`48076a8`，base `b06198f`）

| 环境变量 | 取值 | 适用模块 | 作用 |
|---|---|---|---|
| `OMNI_TTS_FLASH_ATTN` | `1` | TTS | 启用 TTS FlashAttention，decode 加速 |
| `OMNI_VISION_BATCH_ALL` | 默认开 | VPM | overview+slice 同尺寸批量编码 |
| `OMNI_TTS_HEADCODE_NPU` | `1` | TTS | head_code 矩阵乘在 NPU |
| `OMNI_T2W_N_TIMESTEPS` | `4` | token2wav | flow matching 4 步（需与 prompt_cache 一致） |
| `OMNI_T2W_F16_CONV` | `0` | token2wav | vocoder 半精度卷积（目标环境负优化，禁用） |
| `OMNI_T2M_SKIP_REDUNDANT_CONT` | `ADD_MUL` | token2mel | 冗余 ggml_cont 消除 |

提交：`submission.zip`（4 文件结构，含 `llama.cpp-omni.zip` = git archive `48076a8`）。

## 3. 保留项（✅ 有效）

| 优化 | 实验 | 效果 | 精度 |
|---|---|---|---|
| 主 LLM FlashAttention | 08-14 | decode -56%，RTF<1 | 无回归 |
| TTS FlashAttention | Sprint 5 / 本轮 | TTS 阶段 -5~7.6% | bit-identical（logits 一致） |
| head_code NPU matmul | Sprint 3 | TTS headcode CPU→NPU | bit-identical |
| VPM 批量编码 | 本轮 | encode -24%（0.147→0.111） | 与串行一致 |
| 4 步 flow matching | 本轮 | token2wav ~-2%（5→4 步） | 官方验证 byte-identical |
| t2m 冗余 CONT 消除 | Sprint 7/13 | t2m -3.9~-7.7% | bit-identical（ADD_MUL 部分） |
| server 有效性指标上报 | 本轮 | audio_ok/vision_ok/media_error | 新增字段，不改计算 |

## 4. 否决项（❌ 已证伪，保留避免重复）

| 优化 | 实验 | 否决原因 |
|---|---|---|
| Q4_0 / Q8_0 量化 | 08-14/15 | RTF 0.84→1.108 直接超标；量化 matmul 额外 launch 开销吞噬带宽收益 |
| executor 复用（SetRepeatable） | 08-14/15 | CANN 9.1.0-beta.1 段错误 / 单次使用，五路验证全部失败 |
| ACL 图捕获 | 08-13 | SPEAK capture 期 H2D 崩溃（EE9999） |
| cache in-place / KV slot-write | Sprint 10/11 | CANN SET 后端不可行（aclnnInplaceCopy strided 写错），t2m +3.3% |
| B-mid（strided concat） | Sprint 8 | 技术可行但性能为负（-640 节点被 strided concat 抵消） |
| vocoder B-lite / F16 conv | Sprint 9 / 本轮 | 图太小收益<1%；F16 conv 目标 CANN 负优化（token2wav +14%） |
| flow B=1（cfg=0 跳过 uncond） | 本轮 | token2wav 0.223→0.178（-16%）、FULL 0.655，但 **TTS 精度评测 reset 崩溃**（CANN），回退 |
| T2W flow 5→3 步 | 08-14 | prompt_cache/图约束导致 GPU init 失败 |
| W8A8 量化 | 08-13 | 每 matmul 8 个 aclnn 启动，launch 墙 |
| ARN 融合 / V5 bias 补偿 | 08-13 | 更慢或 kernel 慢 2.6 倍 |
