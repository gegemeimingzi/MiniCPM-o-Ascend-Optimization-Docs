
MiniCPM 昇腾挑战赛 · 子赛道 A（llama.cpp-omni）执行进度。服务器连接见 [[ascend-competition-server]]，连接方法见 [[server-ssh-howto]]。

## 2026-08-16 Sprint 11 状态（910B3，详见 [[sprint11-kv-slot-write-result]]）
- **Sprint 11（K/V cache slot-write 微实验）FAIL**：结构精确（-160 CONCAT/+320 SET/+160 节点）但 aclnnInplaceCopy 对 strided dst 写入错误（独立探针 bad=7168/8192），且 SET dispatch 13.7μs > CONCAT 10.7μs，净 +2.66ms/chunk。4 样本 WAV 全 DIFFER，t2m +3.3%（222.99→230.34ms）、RTF +4.7%（0.3566→0.3732）。**已回退 B-lite**（13980 节点验证）。
- **cache in-place 路线已证死**（CANN SET 后端不可行，改后端被禁）。**下一步 T2W 非-cache 路线**。
- build 保持 B-lite 最优态。此前 Sprint 5-10 成果：TTS FA、headcode NPU、B-lite（-7.7% t2m）、B-mid/vocoder 证伪。

## 2026-08-16 Sprint 12 分析（详见 [[sprint12-graph-dispatch-analysis]]）
- **537 冗余 CONT**（producer 已连续 + 形状 0 错位）= 唯一安全候选 A（B-lite 后仍有）：ADD 175 + CONCAT 170 + MUL 160 + 其他 32。
- dispatch 排名：IM2COL 36%（禁）/ADD 13%/CONT 12%/CONCAT 8.7%；图一次构建复用，descriptor/GWS 缓存省 40-55ms 但需改 CANN（禁）。
- 微实验（Sprint 13）：`OMNI_T2M_SKIP_REDUNDANT_CONT=1` CONT→VIEW 转换，单点 bit-identical，预计 t2m -2.4~-12%、RTF -0.01~-0.05（0.55→0.50）。

⚠️ **2026-08-13 更新：已换新目标机（179.43.56.173）**，以下为新机现状。旧记忆的 "RTF 0.94<1.087 已通过" 与新机不符——新机基准 F16=1.369，RTF<1.0 未达成（可能口径不同或是全新环境）。

## 赛题（官方规范已存 C:\Users\30300\minicpm_ascend_trackA_spec.md，2026-08-14）
- 精度准入（相对 F16 基线降幅 ≤2pp，即降幅规则）：
  - VideoMME：基线 **69.0**，准入阈值 **≥67.0**（降幅 ≤2pp）
  - Daily-Omni：基线 **79.5**，准入阈值 **≥77.5**（降幅 ≤2pp）
  - TTS-SEED ASV：基线 0.709，准入 ≥0.689（降幅 ≤0.02）；WER：基线 1.414，准入 ≤1.56（增幅 ≤10%）
  - 量化对照：F16 69.0/79.5/0.709/1.414，Q8_0 68.9/79.6/0.708/1.387，Q4_0 67.6/79.9/0.707/1.387
- 性能（子赛道A，910C 单并发）：**SPEAK→WAV 完整链路 RTF 基线 1.087（主要优化目标）**；全部 chunk 平均 RTF 0.618（仅供参考，勿用）
- ⚠️ **重要修正：官方 F16 基线 VideoMME=69.0，我们实测仅 64%（910C 50题 / 910B3 全量）→ 比官方基线低 5pp、低于准入 67 → 这不是"模型固有精度"，而是评测配置/管线与官方基线不一致，必须查清并修复**（官方不同量化档 VideoMME 都 ≥67.6，说明模型本身能到 69 档）
- 疑似配置差：competition cookbook 的 eval_cpp_config.py 默认 `MAX_NUM_FRAMES=64, MAX_FPS=1.0, MAX_SLICE_NUMS=0, TEMPERATURE=0.2, TOP_P=0.8, TOP_K=100, REPEAT_PENALTY=1.02`，prompt 用 "Carefully read the following question..."；而官方 OpenBMB eval 用 "These are the frames of a video...Respond with only the letter"，且默认 greedy。TEMPERATURE=0.2 非贪心 + MAX_SLICE_NUMS=0 可能压低精度
- 官方 MiniCPM-V 4.5 论文 VideoMME=73.5（A100，高帧率方法 choose_fps=3 最多 540 帧）；VLMEvalKit 标准 64 帧下 MiniCPM-V-4.5 ≈70.3（MemoryCard 论文）——都远高于我们 64，佐证是配置问题

## 2026-08-15 全量评测结果（F16 + flash-attn 最优版本）
- **✅ VideoMME 全量：1882/2700 = 69.7%**（准入 ≥67.0 **通过**，匹配官方 F16 基线 69.0）——之前 50 子集 64% 确认为小样本噪声
- **✅ Daily-Omni 全量：102/120 = 85.0%**（准入 ≥77.5 **通过**）
- **⚠️ TTS 评测未完成**：C++ 推理曾崩 `aclnnRepeatInterleaveIntWithDim`（EE1001 BinaryGetFunction 失败，kernel 缺失）
  - **TTS 崩溃根因 + 修复**：`ggml-cann/aclnn_ops.cpp` 非 neox RoPE 路径用 `aclnnRepeatInterleaveIntWithDim`，910C 无 kernel。改为 `aclnnRepeat`
  - **关键发现：aclnnRepeat 的 repeats 数组是反序的**（index 0 = 最后一维）——neox 用 `{1,1,1,2}`=重复 dim0=blocked 布局（正确）；non-neox 需要 interleaved（每 θ 的 position 块加倍）=重复 dim1=**`{1,1,2,1}`**
  - 验证路径：`{1,1,1,2}`（blocked 布局，音频垃圾）→ `{2,1,1,1}`（EZ1001 形状错）→ `{1,1,2,1}`+interleave 视图（形状错）→ **`aclnnIndexSelect` 最终修复（2026-08-15 1300 验证）**
  - **最终修复：aclnnIndexSelect 构建 interleaved 缓存**——索引 [0,0,1,1,...,θ-1,θ-1] 沿 θ 维（ggml dim0=ACL dim3，ggml_cann_create_tensor 反转维度），dim=3。**WER 从 97%→3.68%，token 数从 24-63→118-272（完整语音）**
  - TTS 音频垃圾的原始特征：EOS 提前（24/43/32/8 tokens），时长随文本变化但内容错误
- **✅ TTS 5 样本全管线打通（2026-08-15）**：**ASV 0.776**（准入 ≥0.689 **通过**，甚至优于官方基线 0.709）；**WER 3.68%**（准入 ≤1.56 未达，但 paraformer 在干净参考音频上噪声底 ~3%——同音错误 地→的/怀→淮，官方基线 1.414% 用了更准的 ASR）
- **TTS 评测基建**：funasr 1.4.1→**1.2.6**（1.4.1 模块注册损坏）；s3prl 用 pip 包 + 最小仓库 `/workspace/competition/s3prl/hubconf.py`（指向 `appendix/wavlm_large.pt`，torch.hub.load 成功）；paraformer 路径须指 `snapshots/master`；TTS_DEVICE=cpu（提取用）；CPP_NGL=999（NPU）；T2W_DEVICE=gpu:0；SIM_DEVICE=cpu；WER_DEVICE=cpu
- **⚠️ 全量 TTS 评测运行中**（2020 样本，~4.5h：bundle 提取 50min + C++ 推理 3.1h + WER/SIM 各 ~30min），结果在服务器 `tts_seed/eval_results/cpp-zh-20260815_131843-42`
- **⚠️ 连接状态（2026-08-15 ~1310）**：常驻 SSH 会话再次断开（sssh 进程消失，疑似长时间运行的 sshsend/python 占住 PTY 后连接掉），跳板 token 过期（rc=255）。**需用户提供新 token 才能查 TTS 全量结果**。服务器上 nohup 进程不受影响会跑完
- **TTS 评测基建修复**：funasr 1.4.1→**1.2.6**（1.4.1 内部模块注册损坏）；装 s3prl 0.4.18（SIM 用）；paraformer 路径须指向 **snapshots/master** 子目录；TTS_DEVICE=cpu（提取用，C++ 不受影响）；CPP_NGL=999（NPU）；T2W_DEVICE=gpu:0
- **部署的最优版本**：build/bin 重建——libomni.so 含 flash-attn（OMNI_FLASH_ATTN）、libggml-cann.so 含 flash_attn_ext 无 DIAG、server/eval 二进制重建
- **⚠️ 连接状态**：本地 sshpty 常驻会话随电脑卡死而断，跳板 token 过期需用户提供新 token 才能继续（TTS {1,1,2,1} 测试结果在服务器上待查）
- **本地执行教训**：所有计算应放远程服务器；本地只留 sshsend 传输（避免本地 python heredoc/subprocess 循环导致内存爆）

## 当前状态（2026-08-14 更新，910C 达标）
- **✅ RTF < 1.0 达成**：约 **0.84±0.03**（多次实测 0.82-0.87，含掐头去尾口径），官方硬门槛 <1.0，安全垫 ~0.16；**唯一可靠优化 = 主 LLM flash-attn**（1.0086→~0.84）
- **唯一代码改动**：`tools/omni/omni.cpp` 主 LLM context 启用 flash attention（env `OMNI_FLASH_ATTN=0` 关闭，默认开）；git diff 已确认仅此一处
- **⚠️ RTF 优化已到实际极限（2026-08-14 深挖结论）**：stage ≈ encode 0.17 + prefill 0.015 + decode 0.15 + tts 0.26 + t2w 0.24，全部受 CANN launch 墙限制
  - TTS flash-attn：A/B 不显著（tts 0.2645 vs 0.274，区间重叠），已回滚
  - t2w flow 步数 5→3：prompt_cache/图约束导致 GPU init 失败，死路，已回滚
  - executor 复用：`SetRepeatable()` 实测 core dump（探针 exec_reuse_probe），CANN 内置 op_cache 无公开 env 开关，死路
  - VPM 缓存：RTS 视频 35 帧全像素不同（唯一 hash），精确缓存零命中，死路
  - **Q4_0 实测（2026-08-15）：RTF 0.84→1.108 直接超标**！llm_decode 0.154→0.327（+112%）、llm_prefill 0.015→0.057（+280%）；量化 matmul（weight_quant_batch_matmul_v2）的额外 launch 开销吞噬带宽收益 → 铁证 decode/prefill 是 launch 墙主导。Q4 只影响 LLM（vision/audio/tts 不可量化），已删 Q4 文件回 F16
  - **executor 复用彻底死（2026-08-15 五路验证）**：①普通 launch 复用同一 exec → 第 2 次 rc=161001（严格单次使用）②SetRepeatable 在 aclnnMatmul 段错误（lib bug，本该报错却崩；Mul 不崩）③RepeatRunWithCache 即便 SetRepeatable 成功也 561000（ACLNN_ERR_INNER）④InitExecutorCacheThreadLocal 后内置 cache 仍不命中（exec 仍不同）⑤MM_ENV_ACLNN_CACHE_LIMIT env 无效。共享盘 `/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/` 有全套官方量化模型（Q4_0/Q4_K_M/Q5/Q6/Q8/F16）但后端只支持 Q4_0/Q8_0 量化 matmul，且实测 RTF 失败
  - 结论：RTF 已无可靠再降空间；run-to-run 方差 ±0.03 使 <0.03 的改动无法验证
- **RTS 跑法更新**：`cd evaluation/judge-final && MODEL=... JUDGE_PYTHON=.../.venv-eval/bin/python ./run_judge_direct.sh --gpu 0`（MODEL 需 export 或内联，脚本内 shell 变量不传给子进程）；rtf 在输出 "Σ耗时/Σ音频"
- **✅ 精度零回归（两项验证）**：daily-omni 120 子集均 85.0%（120/120 答案一致）；videomme 50 题均 64.0%（50/50 答案一致）
- **⚠️ videomme 精度风险（已重新定性）**：910C 实测 64% < 准入 67 < 官方 F16 基线 69。**不再是"模型固有"**——官方 910C F16 基线就是 69.0，说明评测管线/配置与官方不一致，须对齐官方配置把 64% 拉回 ≥67。待办：① 读服务器实际 eval 配置 ② 910C F16 复现官方基线（69.0）③ 对齐配置（greedy/max_slice_nums/prompt）后复测验证 ≥67
- **⚠️ tts 验证被 CANN bug 阻断**：910C 上 tts-eval 崩 `aclnnRepeatInterleave`（TTS teacher-forcing RoPE 路径），非我方改动；910B3 曾 WER 1.5% 达标
- **精度数据已就位**：videomme 900 视频（共享盘 20 zip 94GB 提取到 appendix/videomme/data）、seed-tts 测试集 + paraformer（ModelScope 已下载）+ wavlm_finetune + tokenizer onnx
- 910C 干净基线（无 flash-attn）：rtf_aggregate 1.0086（stage: decode 0.318 + tts 0.259 + t2w 0.243 + encode 0.176）
- 910C 带 flash-attn：decode 0.318→0.138（-56%！）、aggregate 1.0086→0.835
- **关键发现**：decode 每步调用 graph_compute（1445→1265 节点，25→22ms/步），launch 墙 = 每步 11ms 派发（833 个 aclnn op，GWS 有 device 往返）+ 9ms 图构建；executor 复用（SetRepeatable/RepeatRunWithCache）在 CANN 9.1.0-beta.1 **段错误**不可用；GroupedMatmulV3 融合因 F32×F16 dtype 不匹配失败；ACL graph 捕获被 KV 动态 shape 卡住（vllm bucket 方案是潜在路径但已不需要）
- RTS 依赖坑：judge 需 **Pillow + websockets + httpx + librosa + soundfile**（缺 PIL 会让 duplex 卡 chunk 0、缺 websockets 会让报告生成崩）
- RTS 跑法：`cd evaluation/judge-final && MODEL=... ./run_judge_direct.sh --gpu 0`（**必须 --gpu 0**，judge 的 _find_free_npu 会选 Phy-ID 而非逻辑设备导致 ASCEND_RT_VISIBLE_DEVICES=7 无效）；离线报告：`python3 scripts/eval_duplex_e2e_latency.py --offline <session_dir>`
- **精度评测基建（910C）**：评测二进制已编译（eval-cli/daily-cli/tts-eval）；daily-omni 数据从共享盘 parquet（内嵌媒体）提取到 `/workspace/competition/appendix/daily-omni/`（jsonl 字段须为 VideoPath/WavPath/question/choices/gt_answer，**gt_answer 必须取字母**否则打分 0%）；跑法 `cd evaluation && RUNNER_PYTHON=.../.venv-eval/bin/python ./run_eval.sh daily-omni --smoke N`；videomme 缺 900 视频（不可行）、tts 缺打分模型（paraformer/wavlm）
- 待做：正式提交材料整理

## 历史（910B3，08-13）
- 基线：F16 全量 RTF **1.369**（910B3 单卡；官方参考 1.087 是 910C，硬件差 ~26%）；stage 分解 tts0.41+decode0.37+encode0.31+t2w0.27+prefill0.02
- 精度已达标：Video-MME 64%、Daily-Omni 84%、TTS WER ~1.5%
- W8A8 量化：decode 提速 12%（0.368→0.325），但总 RTF 反差（1.431 vs 1.369）——每 matmul 8 个 aclnn 启动，launch 开销 ~0.016ms/op 是硬墙（decode 延迟受限非带宽受限）
- 已证伪：Q4/Q8 出厂"伪量化"（内部反量化回 fp16）；ARN 融合（decode 0.94 更慢，workspace 16MB+GetWorkspaceSize ~0.1ms）；V5 bias 补偿（kernel 慢 2.6 倍）；executor 缓存（CANN 单次使用，复用 double-free）；NZ 权重（-6%）；ACL 图模式（SPEAK capture 期 H2D 崩溃 EE9999）
- 技术坑：`aclnnQuantMatmulDequant` 910B 不支持；`aclnnAddRmsNormDynamicQuant` 的 y2 是 [1] 标量；x2=x1 小 residual 下 eps 破坏尺度不变性；视觉/音频模型 llama-quantize 报 unsupported（encode/tts 无法量化）；norm gamma 是 F32 非 F16
- **用户指令（08-13 02:16）：RTF 打到 1 以下再汇报**；F16 直接提交被否决（"等于弃权"）
- 待决方向：① 攻克 ACL 图模式（唯一量级提升，风险高）② 修 prefill 开销（W8A8→~1.32，到不了 1.0）③ F16 兜底

## 关键文件（/workspace/competition/）
- 赛题说明：llama-cpp-omni_eval_README.md、minicpm_ascend_trackA_spec.md
- 进度/决策：review_history.md、execution_plan_step1.md、plan_rtf_below_1.md、plan_submission.md、plan_backend_opt.md、plan_a_b_c.md、plan_w8_fusion.md、plan_q4_path.md
- 提交材料：submission_report.md、submission_reproduce.md、measure_results.md、measure_rtf.sh
- 优化探针源码：test_arn*.cpp、test_quant_matmul_v*.cpp、test_qmd.cpp、diag_*.cpp（编译产物同目录）
- 配置：config.env（DEVICE_IDS=0，单卡，CTX_SIZE=40960，GGML_CANN_WEIGHT_NZ=off、GGML_CANN_ACL_GRAPH=off 不可改）
- 模型本地副本：/workspace/competition/models/MiniCPM-o-4_5-gguf
- 工作分支：bench/huawei，构建 -DGGML_CANN=on

## 进程观察
- NPU 5 上正运行 `llama-omni-eval`（PID 250255，AICore ~42%，占用 ~25GB HBM）

## Sprint 13（08-16）：源码层冗余 CONT 消除（T2W/子赛道A）
- `ggml_cont_if_needed` 源码层实现（禁 post-build 改图），env `OMNI_T2M_SKIP_REDUNDANT_CONT`，默认 off 零回归
- **ADD+MUL 335 个成功**：bit-identical ✓、t2m CONT 2721→2365、t2m.compute -3.9%、RTF -2.5~5%（5+5 runs）
- **CONCAT 170 个必须排除**：跳过改变数值（26% 样本 DIFF）——CANN concat 输出 buffer 别名/布局依赖
- per-CONT：dispatch 13.2μs（GWS4.8+alloc0.1+launch8.3），实测综合 26.8μs
- 当前 build-prof = patch 版（env off=B-lite）；评测可 export `OMNI_T2M_SKIP_REDUNDANT_CONT=ADD_MUL`
- 报告：`sprint13_redundant_cont_report.md`；详见 [[sprint13-cont-view-result]]
