# 昇腾比赛服务器常驻 SSH 连接方案（GoAnywhere 跳板）

> 适用场景：比赛服务器 SSH 需要经过 **GoAnywhere 跳板机**（`113.47.8.48:2234`），且跳板有严格限制（只放行交互式会话、token 滚动失效）。本文给出一种**单条常驻 SSH 会话 + FIFO 命令通道**的方案，实现快速、免交互、自动重连的远程操作。

---

## 1. 背景与问题

连接链路：

```
本地(Windows Git Bash) ──SSH─→ 跳板 GoAnywhere(113.47.8.48:2234) ──direct-tcpip─→ 目标机(199.103.53.195)
```

跳板有两个硬限制：

| 限制 | 现象 | 结论 |
|---|---|---|
| 只放行"已建立的交互式会话" | `scp` / `rsync` / 非 PTY 的 `ssh` 均报 `platform authorization denied`，`stdio forwarding failed` | 必须用 `ssh -tt`（分配伪终端）伪装成交互终端 |
| token 滚动失效 | 连接一段时间后（经验几小时~1 天）报 `authorization denied`，需换新 token | 需要自动重启 + 快速换 token 的机制 |

因此不能简单"每跑一条命令连一次"（慢、且非 PTY 被拒），而是**建立一条持久的交互式 SSH 会话**，通过本地 FIFO 持续发命令、读输出。

---

## 2. 总体架构

```
┌──────────────────────────── 本地（Windows）────────────────────────────┐
│                                                                       │
│  sshpty.sh（常驻进程，一条持久 ssh）                                     │
│    ssh -tt ... 'bash --norc -s'  <--stdin── /tmp/ssh_in (FIFO)        │
│          │                                                           │
│          └──stdout/stderr──▶ /tmp/ssh_out (LOG 文件)                   │
│                                                                       │
│  sshsend.sh（每次调用：写命令到 FIFO → 读 LOG 增量 → 取回输出）            │
│  ssht.sh    （单次连接备用，临时用）                                      │
│  upload_chunks.sh（base64 分块上传）                                     │
└───────────────────────────────────────────────────────────────────────┘
                          │ SSH (-tt PTY)
                          ▼
                   跳板 GoAnywhere
                          │ direct-tcpip
                          ▼
                    目标机 bash -s
```

**一条 ssh 进程是唯一会话**；所有命令都从 FIFO 喂给它，输出统一落盘，由 sshsend 按 marker 取回。避免了每次命令都新建 SSH 连接（快），也让长任务/长输出不受本地命令超时影响（只要不长期占住 PTY）。

---

## 3. 核心原理

### 3.1 为什么必须 `-tt`（伪终端）

跳板只放行**交互式会话**（`direct-tcpip`，等价于用户手动敲 `ssh`）。`-tt` 强制分配 PTY，让本地连接看起来像真实交互终端。实测：

- `ssh 目标机 "cmd"`（无 PTY）→ 被拒 `platform authorization denied`
- `ssh -tt 目标机 "cmd"` → 放行

（副作用：输出会带 PTY 转义码/回车，sshsend 里用 perl 清洗。）

### 3.2 FIFO 双向通道（核心）

```bash
mkfifo /tmp/ssh_in                 # 命名管道：命令入口
tail -f /dev/null > /tmp/ssh_in &  # 保持写端打开，否则 FIFO 无写者时 ssh 读 EOF 退出
ssh -tt ... bash --norc -s < /tmp/ssh_in > /tmp/ssh_out 2>&1
```

- 远程跑 `bash --norc -s`：从 stdin 读命令的 shell（无 rc 文件干扰）
- 本地写 `echo xxx; cmd; echo yyy` 到 FIFO → 远程执行 → 输出进 `/tmp/ssh_out`
- **关键点**：`tail -f /dev/null > FIFO &` 保一个写端，防止 FIFO 因无写者而让 ssh 读到 EOF 退出

### 3.3 心跳保活

```bash
while true; do sleep 20; printf '#ka\n' > FIFO 2>/dev/null; done
```

每 20s 写一条 bash 注释（无操作、几乎零输出），让跳板/服务器认为连接活跃，防止空闲超时断开。SSH 自身再叠加 `ServerAliveInterval=25 - ServerAliveCountMax=8`（25s 探测、8 次失败才断）双保险。

### 3.4 自动重启 + token 死亡检测

```bash
while true; do
  ssh -tt ... 'bash --norc -s' < "$FIFO" > "$LOG" 2>&1
  rc=$?
  if grep -qE "Permission denied|authorization denied" "$LOG"; then
    echo "TOKEN DEAD, stopping"; break   # token 过期，停下等你换新 token
  fi
  sleep 3                                # 网络抖动断了 → 3s 后自动重连
done
```

- SSH 进程意外退出 → sleep 3 → 重连（用同一条命令，FIFO/LOG 复用）
- 日志里出现 `authorization denied` → 判定 token 死，停止等待

### 3.5 密码自动注入

```bash
SSH_ASKPASS=$ASK SSH_ASKPASS_REQUIRE=force ssh ...
# ASK 是一个 echo 密码的小脚本
```

配合 `SSH_ASKPASS_REQUIRE=force` 让 SSH 直接从 askpass 脚本读密码，免交互输入。（注意：MSYS2 下不要加 `setsid`，`ControlMaster=no`，否则 askpass 回读会挂。）

### 3.6 sshsend 的 marker 机制

每次调用：

1. 记当前 `/tmp/ssh_out` 字节数（offset）
2. 往 FIFO 写：`echo ">>marker<<"; <你的命令>; echo "<<marker>>"`
3. 轮询 LOG 从 offset 起的增量，直到看到 `<<marker>>`
4. 把这段输出做清洗（去 PTY 转义、去 marker 行、去提示符行），返回

这样每次往返是**一次性请求-响应**，互不干扰；默认等 60s（可传第二个参数加长）。

---

## 4. 脚本全集（可直接复制）

### 4.1 `sshpty.sh` — 常驻会话（核心）

```bash
#!/bin/bash
# sshpty.sh — 常驻 SSH 连接（PTY + 心跳保活，模拟用户交互终端）
# 用前：把 JUMP 换成当前有效 token
TARGET_PASS="你的目标机密码"
JUMP="jt_当前有效token:签名@113.47.8.48:2234"
FIFO=/tmp/ssh_in
LOG=/tmp/ssh_out
ASK=/tmp/askpass_pty.sh

cat > "$ASK" <<EOF
#!/bin/sh
echo "$TARGET_PASS"
EOF
chmod +x "$ASK"

rm -f "$FIFO" "$LOG"
mkfifo "$FIFO"
tail -f /dev/null > "$FIFO" &          # 保持写端打开，防 EOF
KEEP=$!

( while true; do sleep 20; printf '#ka\n' > "$FIFO" 2>/dev/null; done ) &   # 心跳
KEEPALIVE=$!

while true; do
  SSH_ASKPASS=$ASK SSH_ASKPASS_REQUIRE=force \
  ssh -tt \
    -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no \
    -o ConnectTimeout=20 -o ServerAliveInterval=25 -o ServerAliveCountMax=8 \
    -J "$JUMP" root@目标机 'stty -echo; PS1=""; bash --norc -s' < "$FIFO" > "$LOG" 2>&1
  rc=$?
  if grep -qE "Permission denied|authorization denied" "$LOG" 2>/dev/null; then
    echo "TOKEN DEAD, stopping."; break
  fi
  sleep 3
done
kill $KEEP $KEEPALIVE 2>/dev/null
```

启动方式：`nohup ./sshpty.sh > /tmp/sshpty.log 2>&1 &`（后台运行，别关终端）。

### 4.2 `sshsend.sh` — 发命令/收输出

```bash
#!/bin/bash
# 用法: ./sshsend.sh "命令" [等待秒数]
FIFO=/tmp/ssh_in
LOG=/tmp/ssh_out
[ -z "$1" ] && { echo "usage: sshsend.sh <command>"; exit 1; }
[ ! -p "$FIFO" ] && { echo "[sshsend] FIFO 不存在，先运行 ./sshpty.sh"; exit 1; }

# 折叠换行（避免远程 bash -s 语法错误）；修复连接符后多余分号
cmd=$(printf '%s' "$1" | tr '\n' ';' | sed -e 's/&&;/\&\& /g' -e 's/||;/\|\| /g' -e 's/&;/\& /g' -e 's/|;/\| /g' -e 's/^[;[:space:]]*//' -e 's/[;[:space:]]*$//')

marker="M$(date +%s)$$$RANDOM"
offset=$(stat -c%s "$LOG" 2>/dev/null || echo 0)

printf 'echo ">>%s<<"; %s; echo "<<%s>>"\n' "$marker" "$cmd" "$marker" \
  | timeout 6 bash -c 'cat > /tmp/ssh_in' 2>/dev/null
[ $? -ne 0 ] && { echo "[sshsend] WARNING: 写入超时，常驻连接可能已断开（重新运行 ./sshpty.sh）"; exit 2; }

WAIT="${2:-60}"; ITER=$((WAIT * 5)); n=0
while [ $n -lt $ITER ]; do
  sz=$(stat -c%s "$LOG" 2>/dev/null || echo 0)
  if [ "$sz" -gt "$offset" ] && tail -c +$((offset+1)) "$LOG" 2>/dev/null | grep -q "<<$marker>>"; then break; fi
  sleep 0.2; n=$((n+1))
done

tail -c +$((offset+1)) "$LOG" 2>/dev/null \
  | perl -pe 's/\e\[[0-9;?]*[a-zA-Z]//g; s/\e\][^\a]*\a//g; s/\e[=>]//g; s/\r//g' \
  | sed -e "/>>$marker<</d" -e "/<<$marker>>/d" \
  | grep -vE "^root@|^$" | head -c 9000000
```

### 4.3 `ssht.sh` — 单次连接备用

```bash
#!/bin/bash
# 用法: ./ssht.sh "命令"   （每次新建一次 SSH 连接，0.2s，用于 sshpty 不可用时的应急）
TARGET_PASS="你的目标机密码"
JUMP="jt_当前有效token:签名@113.47.8.48:2234"
ASK=/tmp/askpass_ssht.sh
printf '#!/bin/sh\necho "%s"\n' "$TARGET_PASS" > "$ASK"; chmod +x "$ASK"

CMD="${1:-hostname; whoami; uname -a}"
SSH_ASKPASS=$ASK SSH_ASKPASS_REQUIRE=force \
timeout 60 ssh -o StrictHostKeyChecking=no -o ConnectTimeout=20 -o NumberOfPasswordPrompts=1 \
  -o PreferredAuthentications=password -o PubkeyAuthentication=no -o ControlMaster=no \
  -J "$JUMP" root@目标机 "$CMD" < /dev/null
```

> 注意：单次连接也必须走 `-tt` 吗？`ssht.sh` 这里没加 `-tt`，实测**命令模式（`ssh 目标机 "cmd"`）经跳板可通**；被拒的是 scp/非命令模式。若遇到 `platform authorization denied`，给 ssh 加 `-tt` 即可。

### 4.4 上传脚本（跳板拒 scp，用 base64 流）

大文件上传：本地 `base64 -w0 文件` → 分块（每块 ≤1700 字符）经 sshsend 写进服务器 `/tmp/up.b64` → 服务器 `base64 -d` 落盘。分块函数要点：处理 `fold` 最后一块无换行被 `while-read` 丢弃的问题（余数单独补发），并校验远程字节数。

---

## 5. 从零开始连接流程

1. **准备**：本地是 Git Bash（MSYS2）。准备目标机密码 + 当前有效的跳板 token（形如 `jt_xxx:yyy@113.47.8.48:2234`，向管理员/平台索取）。
2. **写脚本**：把上面 4 个脚本保存到本地，把 `TARGET_PASS` 和 `JUMP` 改成你的值（`JUMP` 每换 token 要更新）。
3. **启动常驻会话**：
   ```bash
   nohup ./sshpty.sh > /tmp/sshpty.log 2>&1 &
   sleep 12   # 等 SSH 握手完成
   ```
4. **验证**：
   ```bash
   ./sshsend.sh "hostname; whoami"
   # 应输出目标机主机名和 root
   ```
5. **日常使用**：`./sshsend.sh "任意命令"` 即可。长命令（评测、下载）传第二个参数加长等待：`./sshsend.sh "./run_eval.sh rts" 200`。
6. **换 token**：当 sshsend 报"写入超时/常驻连接断开"，或 sshpty 日志出现 TOKEN DEAD：
   ```bash
   # 1) 用 sed 把 sshpty.sh / ssht.sh / sshsend 里的旧 JUMP 替换成新 token
   # 2) 重启：
   pkill -f sshpty; pkill -f "ssh -tt"; sleep 1
   rm -f /tmp/ssh_in /tmp/ssh_out
   nohup ./sshpty.sh > /tmp/sshpty.log 2>&1 &
   sleep 12 && ./sshsend.sh "hostname"
   ```

---

## 6. 优缺点

### 优点

| 项 | 说明 |
|---|---|
| **快** | 只建一条连接，后续命令零握手（实测命令往返 ~0.1-0.2s） |
| **免密码** | askpass 自动注入，全程无交互 |
| **自动重连** | 网络抖动断了 3s 自动重连；token 死有明确信号 |
| **能跑长输出** | 输出落盘文件，sshsend 按 marker 增量取回，不受 ssh 输出管道限制（大输出需分块或加长等待） |
| **存活久** | 心跳 + keepalive，token 有效期内稳定 |

### 缺点

| 项 | 说明 |
|---|---|
| **token 滚动失效** | 几小时~1 天过期，需手动更新 JUMP 并重启（最大痛点） |
| **单条会话、单点** | 所有命令共用一条 PTY；长任务长时间占住 PTY 可能挤掉连接 |
| **大文件传输慢** | 跳板拒 scp，只能 base64 分块（51MB 约需 19 块，几分钟） |
| **依赖本地进程** | sshpty 是本地进程，电脑重启/卡死/被杀会断线 |
| **PTY 输出有转义** | 需清洗转义码/回车/提示符（脚本已处理） |
| **Windows 特有限制** | MSYS2 下禁 `setsid`、禁 `ControlMaster`（askpass 会挂） |

---

## 7. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| `[sshsend] 写入超时` | 常驻连接断开 / token 死 | 看 `/tmp/ssh_pty_status` 和 sshpty 日志；换 token 重启 |
| `platform authorization denied` | 非 PTY 连接被跳板拒（scp/无 `-tt`） | 命令模式给 ssh 加 `-tt`；传输走 base64 |
| `TOKEN DEAD` | token 过期 | 换新 token 更新 JUMP，重启 sshpty |
| 命令输出为空/只有提示符 | marker 没等到（大输出超时） | 加长 `./sshsend.sh "cmd" 300`；或分多次 |
| 服务器任务被本地断线影响 | 任务挂在 PTY 上 | 长任务用 `nohup ... &` 脱离会话（如评测、server） |
| FIFO 被占/重复启动 sshpty | 上一次进程没清干净 | `pkill -f sshpty; pkill -f "ssh -tt"; rm -f /tmp/ssh_in /tmp/ssh_out` 后重启 |

---

## 8. 安全提示

- **token 和密码是敏感凭据**：写入脚本是明文，脚本不要提交到公开仓库；换 token 时用 sed 替换即可。
- 服务器上的长任务尽量 `nohup` 脱离会话，断线不影响。
- 本方案仅用于**自己拥有权限的比赛/测试服务器**，不要用于未授权的主机。
